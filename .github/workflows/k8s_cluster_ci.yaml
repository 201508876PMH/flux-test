name: k8s cluster CI and tests

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [ '*' ]
    tags-ignore: [ '*' ]

jobs:
  kubernetes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Flux
        uses: fluxcd/flux2/action@main
      - name: Setup Kubernetes
        uses: helm/kind-action@v1.7.0
        with:
          cluster_name: flux

      - name: Determine Branch
        id: get_branch
        run: |
          # Check if this is a pull request
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            # Use the PR base branch for flux (the branch the PR is merging into)
            echo "BRANCH=${{ github.event.pull_request.base.ref }}" >> $GITHUB_ENV
          else
            # Use the branch name from a normal push event
            echo "BRANCH=${GITHUB_REF#refs/heads/}" >> $GITHUB_ENV
          fi

      - name: Install Flux in Kubernetes Kind
        run: flux install

      - name: Setup cluster reconciliation
        run: |
          flux create source git flux-system \
          --url=${{ github.event.repository.html_url }} \
          --branch=peter/add-metallb \
          --username=${GITHUB_ACTOR} \
          --password=${{ secrets.GHT }} \
          --ignore-paths="clusters/**/flux-system/"
          flux create kustomization flux-system \
          --source=flux-system \
          --path=./flux/bootstrap
          
      - name: Verify cluster reconciliation
        run: |
          kubectl -n flux-system wait kustomization/flux-system --for=condition=ready --timeout=2m
          
      - name: Verify helm reconciliation
        run: |
          kubectl -n podinfo wait helmrelease/podinfo --for=condition=ready --timeout=2m
          kubectl -n metallb-system wait helmrelease/metallb --for=condition=ready --timeout=2m

      - name: Debug failure
        if: failure()
        run: |
          kubectl -n flux-system get all
          kubectl -n flux-system logs deploy/source-controller
          kubectl -n flux-system logs deploy/kustomize-controller
          kubectl -n flux-system logs deploy/helm-controller
          flux get all --all-namespaces
