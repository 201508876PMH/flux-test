apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb-system
spec:
  interval: 5m
  chart:
    spec:
      chart: metallb
      version: "0.13.7"   # Adjust the version as needed
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: flux-system
  values:
      installCRDs: true  # Ensure CRDs are installed
